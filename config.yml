# Configuration for Rasa NLU.
# https://rasa.com/docs/rasa/nlu/components/
language: en

pipeline: "pretrained_embeddings_spacy" # loads the spacy language model

policies:
  - name: MemoizationPolicy
  - name: KerasPolicy
  - name: MappingPolicy
